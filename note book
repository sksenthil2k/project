1. Show AdvancedExcelProcessor.java:

powershell
Get-Content (Get-ChildItem -Recurse -Filter "AdvancedExcelProcessor.java" | Select-Object -First 1).FullName
2. Show CostController.java:

powershell
Get-Content (Get-ChildItem -Recurse -Filter "CostContorller.java" | Select-Object -First 1).FullName
