Step 1 — Get Full Error From Backend
Run this in PowerShell:

powershell
$body = '{"accountId":"default","startDate":"2024-01-01","endDate":"2024-12-31","granularity":"MONTHLY"}'
try {
    Invoke-RestMethod -Uri "http://localhost:8080/awscost/api/generate" `
        -Method POST `
        -ContentType "application/json" `
        -Body $body
} catch {
    $reader = New-Object System.IO.StreamReader($_.Exception.Response.GetResponseStream())
    $reader.ReadToEnd()
}
Step 2 — Check Spring Boot Console
In the IntelliJ / terminal where Spring Boot is running, look for something like this after clicking the button:

ERROR ... in CostContorller ...
Caused by: software.amazon.awssdk.core.exception.SdkClientException
   OR
Caused by: java.io.FileNotFoundException: output/ (No such file or directory)
   OR  
Caused by: com.fasterxml.jackson.databind.exc.InvalidDefinitionException
Copy and paste the full error from the Spring Boot console here.

Step 3 — Quick Checks
Run these and tell me the results:

powershell
# Check AWS credentials
Test-Path "$env:USERPROFILE\.aws\credentials"
# Check application.properties
Get-Content "src\main\resources\application.properties"
# Check if output directory exists
Get-Content "src\main\resources\application.properties" | Select-String "output"
The Spring Boot console log is the ONLY way to know the exact cause of 500. Look at the IntelliJ/terminal window and paste the red error text.
