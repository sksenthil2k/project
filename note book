# STEP 1: Fix application.properties
@"
spring.application.name=Aws Cost
server.servlet.context-path=/awscost
spring.web.resources.static-locations=classpath:/static/
excel.template.file.path = C:\\Users\\143500\\costsheet\\template
excel.template.file.name = template
excel.input.file.path = C:\\Users\\143500\\costsheet\\input
excel.input.file.name = input
excel.output.file.path = C:\\Users\\143500\\costsheet\\output
excel.output.file.name = output
server.error.include-message=always
server.error.include-stacktrace=always
"@ | Set-Content "src\main\resources\application.properties"

# STEP 2: Create folders on C: drive
New-Item -ItemType Directory -Force -Path "C:\Users\143500\costsheet\template"
New-Item -ItemType Directory -Force -Path "C:\Users\143500\costsheet\input"
New-Item -ItemType Directory -Force -Path "C:\Users\143500\costsheet\output"

# STEP 3: Copy input file
Copy-Item "src\main\resources\input.xlsx" "C:\Users\143500\costsheet\input\input.xlsx" -Force

# STEP 4: Rebuild
mvn clean package -DskipTests

Write-Host "ALL DONE - Now restart Spring Boot in IntelliJ"
